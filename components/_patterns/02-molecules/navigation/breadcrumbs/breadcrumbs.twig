<nav class="breadcrumb pt-2 pb-2 mb-1 mt-4 caps h5" role="navigation" aria-labelledby="system-breadcrumb">
  <h2 class="visually-hidden" id="system-breadcrumb">{{ heading|default('You are here') }}</h2>
  <ol class="breadcrumb__list m-0 p-0 list-style-none">
    {% for item in breadcrumb %}
      <li class="breadcrumb__item inline mr-1">
        {% if item.url %}
          <a class="breadcrumb__link mr-1" href="{{ item.url }}">{{ item.text }}</a>
        {% else %}
          {{ item.text }}
        {% endif %}
        {% if not loop.last %}
          ›
        {% endif %}
      </li>
    {% endfor %}
  </ol>
</nav>
