{% set attributes = attributes.addClass([
  'mt-4',
  'mb-1',
  'pt-4',
  'pb-4',
  'pr-4',
  'pl-4',
  'flex',
  'items-center',
  'border',
	type == 'status' ? 'green',
	type == 'warning' ? 'orange',
	type == 'error' ? 'red'
]) %}

<div {% if type == 'error' %} role="alert" {% else %} role="contentinfo" {% endif %} {% if heading %}aria-label="{{ heading }}"{% endif %} {{ attributes }}>

  {% block heading %}
    {% if heading %}
      <h2 class="visually-hidden">{{ heading }}</h2>
    {% endif %}
  {% endblock heading %}

  {% block icon %}
    <div class="messages__icon inline-block mr-1">
      {% if type == 'error' %}
        <i class="icon icon-exclamation-circle"></i>
      {% elseif type == 'warning' %}
          <i class="icon icon-bell"></i>
      {% else %}
          <i class="icon icon-check-circle"></i>
      {% endif %}
    </div>
  {% endblock icon %}

  {% block content %}
    {% if messages|length > 1 %}
      <ul class="messages__list m-0 p-0">
        {% for message in messages %}
          <li class="messages__item">{{ message }}</li>
        {% endfor %}
      </ul>
    {% else %}
      <div class="messages__list">
        {{ messages|first }}
      </div>
    {% endif %}
  {% endblock content %}
</div>
