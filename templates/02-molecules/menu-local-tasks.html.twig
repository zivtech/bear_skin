{#
/**
 * @file
 * Theme override to display primary and secondary local tasks.
 *
 * Available variables:
 * - primary: HTML list items representing primary tasks.
 * - secondary: HTML list items representing primary tasks.
 *
 * Each item in these variables (primary and secondary) can be individually
 * themed in menu-local-task.html.twig.
 */
#}
{% if primary %}
  {%
    include "@molecules/navigation/tabs/tabs.twig" with {
      heading: 'Primary tabs'|t,
      modifier_class: 'tabs--primary nm np inline-block rounded border',
      tabs: primary
    } only
  %}
{% endif %}

{% if secondary %}
  {%
    include "@molecules/navigation/tabs/tabs.twig" with {
      heading: 'Secondary tabs'|t,
      modifier_class: 'tabs--secondary my-4 mx-0 py-2 pr-0 pl-1',
      tabs: secondary
    } only
  %}
{% endif %}
